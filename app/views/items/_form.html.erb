<%= simple_form_for [current_user, item] do |form| %>
  <% if item.errors.any? %>
	<div id="error_explanation">
	  <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

	  <ul>
		<% item.errors.full_messages.each do |message| %>
		  <li><%= message %></li>
		<% end %>
	  </ul>
	</div>
  <% end %>

  <div class="field">
	<%= form.input :name %>
  </div>

  <div class="field">
	<%= form.input :description %>
  </div>

  <div class="field">
	<%= form.input :category %>
  </div>

  <div class="actions">
	<%= form.submit %>
  </div>
<% end %>
