<!-- Jumbotron personnalisé -->
<div class="container py-4">
    <div class="p-5 mb-4 bg-light rounded-3 jumbotron">
        <div class="container-fluid py-5">
            <h1 class="display-5 fw-bold mb-4">LoueMoi</h1>
            <blockquote class="blockquote text-left">
              <p class="col-md-12 fs-4">"Connect with your neighbors and share services effortlessly. Join Louemoi today and build a stronger community!"</p>
            <%= link_to "Signup", new_user_registration_path, class: "btn btn-primary btn-lg"  %>
            </blockquote>
        </div>
    </div>
</div>

<div class="row justify-content-center mt-4">
  <div class="col-md-12">
    <% if @query.present? %>
      <h2>Search Results for "<%= @query %>":</h2>
      <% if @items.any? %>
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3">
          <%= render partial: 'shared/item_card', collection: @items, as: :item %>
        </div>
      <% else %>
        <p>No items found.</p>
      <% end %>
    <% else %>
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3">
        <%= render partial: 'shared/item_card', collection: @items, as: :item %>
      </div>
    <% end %>
  </div>
</div>
